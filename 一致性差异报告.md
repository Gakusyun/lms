# 代码缺失功能清单 — 论文有 / 代码无

> 本清单仅列出**论文描述但代码中尚未实现**的功能，按模块分类，可直接交给编码AI作为实现任务。
> 以实际代码现状为准，排除已有功能（JWT/bcrypt/QR登录/基础CRUD等）和路径差异。
> 核查时间：2026/04/14

---

## 一、认证与安全管理（缺失项）

### 1.1 集中化 RBAC 中间件
- **论文描述**：全量 API 需实现基于角色的访问控制，非法越权请求需被拦截并返回明确错误。
- **现状**：`check_login` 验签已实现，但角色校验逻辑分散在各个 service/endpoint，无统一拦截器。
- **建议**：在 `app/api/deps.py` 或中间件层统一实现角色白名单校验。

---

## 二、请假申请管理（缺失项）

### 2.1 课程冲突检测
- **论文描述**：自动业务校验 — 课程冲突检测。
- **现状**：无任何课程时间冲突检测逻辑。
- **实现要点**：学生提交请假时，查询 `StudentCourse` + `Course` 表，比对请假时段是否与已选课程上课时间重叠。

### 2.2 历史请假频次校验
- **论文描述**：自动业务校验 — 历史请假频次校验。
- **现状**：无任何频次限制逻辑。
- **实现要点**：统计该学生近 N 天请假次数，超过阈值时提示或触发多级审批。

### 2.3 时长分级审批路由
- **论文描述**：自动业务校验 — 时长分级校验（触发多级审批）。
- **现状**：所有请假走同一审批流程，无分级。
- **实现要点**：按请假时长（如 ≤1天 / 1-3天 / >3天）分配不同审核员或增加审批层级。

### 2.4 紧急请假担保人关联校验
- **论文描述**：紧急请假支持担保人机制（关联 `guarantee_student_id`）。
- **现状**：字段存在，但提交申请时无担保人校验逻辑，审核员无法查看关联信息。
- **实现要点**：申请时校验担保学生是否存在且有 `guarantee_permission`，审核员端展示担保人信息。

---

## 三、审批流程管理（缺失项）

### 3.1 批量审批
- **论文描述**：审核员支持单条与批量审批请假申请。
- **现状**：仅有 `POST /leaves/approve/{id}` / `reject/{id}` 单条操作，无批量接口。
- **实现要点**：
  - 后端：新增 `POST /leaves/approve/batch` 接收 `leave_ids` 列表
  - 前端：列表页增加多选 Checkbox + "批量审批" 按钮

### 3.2 智能审批推荐
- **论文描述**：内置智能推荐模块，依据事由、频次、课程权重生成辅助审批建议。
- **现状**：无任何推荐逻辑。
- **实现要点**：审核员进入审批详情页时，后端根据学生历史请假频次 + 请假类型 + 课程重要性打分，给出"建议通过 / 建议驳回 / 建议核实" 结论。

### 3.3 审批消息通知
- **论文描述**：操作需实时触发状态同步与消息通知。
- **现状**：审批后状态同步到数据库，但无任何通知推送（无 WebSocket/SSE/微信模板消息）。
- **实现要点**：审核结果变更后，通知学生端刷新状态（前端轮询或 WebSocket 推送）。

### 3.4 审批通过后自动生成请假二维码
- **论文描述**：审批通过后，系统自动为该请假记录生成唯一数字凭证（二维码）。
- **现状**：无此逻辑，QR 码仅用于登录阶段。
- **实现要点**：见第五节"二维码凭证与核验模块"。

---

## 四、二维码凭证与核验模块（完全缺失 🔴）

> 本模块是论文核心亮点功能，代码中**完全不存在**，需从前端到后端全链路实现。

### 4.1 请假凭证二维码生成
- **论文描述**：仅对"已批准"状态的请假记录生成唯一、加密的二维码。
- **现状**：无任何 leave QR 相关代码。
- **实现要点**：
  - 后端：审批通过时调用 `qrcode` 库生成二维码，内容为加密的 leave_id + student_id + 有效期时间戳
  - 数据存储：`Leave` 表增加 `qr_code` 字段（存储 base64 或 URL）
  - 前端：学生请假详情页展示该二维码

### 4.2 二维码有效期与使用次数限制
- **论文描述**：二维码需设定明确的有效时间区间与使用次数限制。
- **实现要点**：
  - `Leave` 表增加 `qr_valid_from`、`qr_valid_until`、`qr_max_uses`、`qr_use_count` 字段
  - 生成二维码时计算有效期（如请假当天开始，至当晚23:59）
  - 使用次数限制默认设为 1（仅核验一次）

### 4.3 教师扫码核验 API
- **论文描述**：核验端扫码后，后端解密验证并返回：学生信息、请假时间、审批状态、有效性判定。
- **现状**：无此 API。
- **实现要点**：
  - 新增 `POST /leaves/verify-qr` 接收扫码内容
  - 解密 QR 内容 → 查询 Leave 记录 → 校验状态/有效期/次数 → 返回验证结果
  - 返回示例：`{valid: true, student_name: "张三", leave_date: "2026-04-15", status: "已批准"}`

### 4.4 核验结果拦截与错误提示
- **论文描述**：严格拦截过期、伪造、重复使用或状态异常的二维码，返回对应错误提示。
- **实现要点**：
  - 错误码体系：`QR_EXPIRED` / `QR_USED_UP` / `QR_INVALID_SIGNATURE` / `QR_STATUS_ABNORMAL`
  - 每次核验记录核验日志（时间/教师ID/leave_id/结果）

### 4.5 小程序端请假二维码展示
- **论文描述**：学生需能展示请假二维码给教师核验。
- **现状**：小程序无任何请假凭证展示功能。
- **实现要点**：
  - 小程序请假详情页增加"展示请假凭证"按钮，点击展示二维码
  - 教师端小程序增加"扫一扫核验"功能（`wx.scanCode` → 调用 `/leaves/verify-qr`）

---

## 五、数据管理与统计分析（缺失项）

### 5.1 JSON 格式数据导出
- **论文描述**：多维度数据导出格式需包含 XLSX 与 JSON。
- **现状**：仅有 `excelExporter.ts` 导出 XLSX，无 JSON 导出。
- **实现要点**：
  - 后端：新增 `/export/leaves/json` 等接口，返回 JSON 数据流
  - 前端：导出按钮增加"导出JSON"选项

### 5.2 数据可视化统计图表
- **论文描述**：自动生成缺勤率、请假趋势、各学院/课程统计图表。
- **现状**：后端有统计接口（`/statistics/leaves`、`/statistics/leaves/trend` 等），但**前端无图表渲染**。无 ECharts/chart.js 依赖。
- **实现要点**：
  - 前端引入 ECharts
  - HomePage 或独立 Statistics 页面实现折线图（请假趋势）、饼图（请假类型占比）、柱状图（各学院统计）

### 5.3 新生信息批量导入
- **论文描述**：支持批量操作 — 新生信息批量导入。
- **现状**：无批量导入学生数据的功能。
- **实现要点**：
  - 后端：新增 `POST /students/import` 接收 Excel/CSV 文件，批量解析并创建 Student 记录
  - 前端：学生管理页增加"批量导入"按钮，上传 Excel 模板

### 5.4 历史请假批量处理
- **论文描述**：积压历史请假批量处理。
- **现状**：无此功能。
- **实现要点**：
  - 审核员批量选择历史请假记录，批量审批或标记处理状态

---

## 六、操作日志审计（缺失项 🔴）

### 6.1 审计日志数据表
- **论文描述**：记录关键操作时间、行为、结果及关联对象，支持按时间/角色/类型检索与告警。
- **现状**：无任何审计表或操作日志。
- **实现要点**：
  - 新建 `AuditLog` 模型：`log_id`, `user_id`, `user_role`, `action`, `target_type`, `target_id`, `detail`, `ip_address`, `timestamp`
  - 需记录的敏感操作：登录/登出、密码修改、审批操作、数据导出、用户增删改

### 6.2 管理员审计日志查询界面
- **论文描述**：管理员可通过 Web 端多维检索审计日志。
- **现状**：无审计日志页面。
- **实现要点**：
  - 新增 `AuditLogsList.vue` 页面，支持按时间范围/用户/操作类型筛选
  - 导出审计日志功能（供合规审查）

---

## 七、前端 Web 管理端（缺失项）

| # | 功能 | 说明 |
|---|------|------|
| 1 | 数据统计可视化面板 | 当前仅有数字统计卡片，需接入 ECharts 图表 |
| 2 | 日志审计页 | 无独立审计日志页面，需新建 |
| 3 | 批量审批 UI | 列表页无多选，需增加 Checkbox + 批量操作 |
| 4 | 审批推荐展示 | 审批详情页无智能建议展示区域 |
| 5 | JSON 导出入口 | 导出按钮仅有 Excel 选项 |

---

## 八、微信小程序端（缺失项）

| # | 功能 | 说明 |
|---|------|------|
| 1 | 请假二维码展示 | 审批通过后学生需能展示凭证二维码，无此功能 |
| 2 | 教师扫码核验 | 教师扫码核验请假凭证，无此功能 |
| 3 | 审批状态实时推送 | 请假状态变更后无通知，需轮询或 WebSocket |
| 4 | 分包加载优化 | 当前所有页面单包加载，无分包配置 |

---

## 九、实现优先级建议

| 优先级 | 模块 | 理由 |
|-------|------|------|
| 🔴 P0 | 二维码凭证核验模块 | 论文核心亮点，完全缺失，影响核心业务流程 |
| 🔴 P0 | 操作日志审计系统 | 安全合规底线，管理员必需 |
| P1 | 批量审批 | 审核员核心效率功能 |
| P1 | 业务校验（冲突/频次/分级） | 论文明确要求的自动校验逻辑 |
| P2 | 数据可视化图表 | 统计分析必需，已有后端接口，仅缺前端渲染 |
| P2 | JSON 导出 | 与 XLSX 导出同等重要 |
| P2 | 智能审批推荐 | 辅助决策，算法复杂度适中 |
| P3 | 批量数据导入 | 数据初始化工具 |
| P3 | 小程序分包优化 | 性能优化项 |

---

> 清单共 **9 大模块，24 项缺失功能**，可直接拆分给编码AI逐项实现。
