# Leave Management System - Bug List & Requirements

## 已发现的 Bug

### BUG #1: 课程管理 API 500 错误（已修复）
- 位置: Backend/app/api/v1/courses.py
- 原因: 条件分支内的局部 import 导致 UnboundLocalError
- 修复: 移除函数内的局部 import，使用模块级 import
- 状态: 已修复

### BUG #2: 数据统计页面无数据（已修复）
- 位置: ECharts canvas 渲染不可见
- 修复: 确认正常渲染，Playwright 不可见是 Canvas 特性
- 状态: 已修复（非 Bug）

### BUG #3: 全局异常处理器缺少 CORS headers（已修复）
- 位置: Backend/app/main.py 异常处理器
- 修复: 在 JSONResponse 中添加 CORS headers
- 状态: 已修复

### BUG #4: 学生端"教师管理"显示为0
- 位置: 学生首页 → 教师管理卡片 → 点击后显示0
- 期望: 学生应能看到自己所学课程的所有老师
- 状态: 待修复

### BUG #5: 学生端"审核员管理"显示为0
- 位置: 学生首页 → 审核员管理卡片 → 点击后显示0
- 期望: 学生应能看到自己的辅导员（审核员）
- 状态: 待修复

### BUG #6: 学生端课程管理卡片显示300，但页面可能显示正确
- 位置: 学生首页 → 课程管理卡片显示"300"
- 期望: 卡片数字应显示学生选择的课程数量（非300）
- 注意: 需验证课程页面本身是否正确过滤
- 状态: 待修复

### BUG #7: 教师端课程管理卡片显示300
- 位置: 教师首页 → 课程管理卡片显示"300"
- 期望: 卡片数字应显示教师教授的课程数量（实际只有20门）
- 注意: 课程管理页面本身已正确过滤（只显示教师自己的20门课），仅卡片数字错误
- 状态: 待修复（仅需修复首页卡片计数）

### BUG #8: "查看凭证"按钮在审核员端显示
- 位置: 审核员请假条列表 → 已批准的请假 → 操作栏有"查看凭证"
- 期望: 查看凭证（二维码）只应在学生端显示，审核员/管理员不应看到
- 状态: 待修复

### BUG #9: 教师端首页显示不相关的管理入口
- 位置: 教师首页显示"学生管理(0)"、"审核员管理(0)"、"教师管理(0)"卡片
- 现象: 教师端请求 /students/count 返回 403 Forbidden，产生5个控制台错误
- 期望: 教师只负责授课，应隐藏"学生管理"、"审核员管理"、"教师管理"卡片
- 状态: 待修复

### BUG #10: 管理员个人资料只能修改密码
- 位置: /profile 页面
- 期望: 管理员应该能修改所有信息（姓名等），不仅是密码
- 状态: 待修复

### BUG #11: GenericCreateModal 错误消息不显示
- 位置: Frontend/src/components/GenericCreateModal.vue 行213、304
- 原因: 使用 `error.response?.data?.message` 但后端返回 `error.response?.data?.detail`
- 现象: 创建失败时（如学生未选该课程）用户看不到错误提示，弹窗直接关闭
- 修复: 将 `.message` 改为 `.detail`（或使用 `.detail || .message`）
- 状态: 待修复

### BUG #12: 修改密码错误时无提示
- 位置: /profile 页面的修改密码功能
- 现象: 输入错误旧密码时，弹窗直接关闭，无错误提示
- 状态: 待修复

### BUG #13: /notifications 路由不存在
- 位置: 前端路由配置中缺少 /notifications 路由
- 现象: 访问 /notifications 显示空白页，控制台报 "No match found for location with path '/notifications'"
- 注意: 后端 notifications API 已存在（GET /notifications, /unread-count, /{id}/read, /read-all）
- 状态: 待修复

### BUG #14: 管理员修改自己的密码不需要验证旧密码
- 位置: Backend/app/services/auth.py 行153-154
- 原因: 代码中 `if current_user["role"] != "admin":` 跳过旧密码验证，但这也适用于管理员修改自己的密码
- 现象: 管理员可以不经旧密码验证就修改自己的密码（如测试中输入错误旧密码却成功修改）
- 期望: 管理员修改自己的密码也应验证旧密码；只有修改他人密码时可跳过
- 状态: 待修复

### BUG #15: 测试数据密码不一致
- 位置: 数据库中各角色用户密码不统一
- 现象: admin/reviewer/teacher 密码为 "1"，student 密码为 "123456"
- 期望: 所有用户统一使用密码 "1"
- 状态: 待修复（重新运行测试数据脚本）

---

## 新需求

### 需求1: 审核员分级权限体系（重要 - 需重构）
- 审核员分为三种角色：辅导员、学院书记、学工处
- 辅导员：负责本学院直属学生，可审批1天（8课时）以内请假
- 学院书记：负责整个学院，可审批2至7天请假
- 学工处：负责整个学校，可审批7天（56课时）以上请假
- 学生的审核员栏只应显示本学院的辅导员
- 需要修改数据库模型，增加审核员类型/级别字段
- 需要修改请假审核逻辑，根据课时自动路由到对应级别审核员
- 需要修改插入测试数据的脚本，使数据更真实（包含学院、专业等信息）

### 需求2: 上传证明文件
- 请假时应能上传证明文件（如病假证明、公假文件等）
- 需要文件上传功能（后端 + 前端）
- 需要考虑文件存储、大小限制、格式限制

### 需求3: 辅导员销假功能（完成请假闭环）
- 完整请假流程：学生请假 → 审核 → 销假
- 由辅导员负责销假操作
- 需要新增销假功能（后端 + 前端）
- 销假后状态变更：已批准 → 已销假

### 需求4: 数据统计卡片不显示数字0
- 数据统计页面首页的统计卡片应改为其他展示方式，不显示"0"
- 可改为图标、描述文字等

---

## 测试进度

### 已测试功能
- [x] 登录功能 (Admin/Student/Teacher/Reviewer) - 全部正常
- [x] Admin: 请假条列表（分页、导出Excel、创建请假条弹窗、审核弹窗、撤销确认、查看凭证）
- [x] Admin: 学生管理（列表、创建、编辑、删除弹窗）
- [x] Admin: 教师管理（列表、创建、编辑、删除弹窗）
- [x] Admin: 审核员管理（列表、创建、编辑、删除弹窗）
- [x] Admin: 课程管理（列表、创建、编辑、删除弹窗）
- [x] Admin: 审计日志（筛选、搜索、重置、分页）
- [x] Admin: 数据统计（饼图、折线图、柱状图）
- [x] Admin: 个人资料页面（修改密码弹窗）
- [x] Student: 请假条列表（查看、创建、编辑、撤销、查看凭证）
- [x] Student: 创建请假条（表单填写、课程下拉正确显示自己选的课程）
- [x] Student: 查看凭证（QR码弹窗）
- [x] Teacher: 登录和首页（课程管理页面正确过滤，仅首页卡片数字错误）
- [x] Reviewer: 登录、首页、审核请假条（批准操作正常）

### 已确认正常的页面/功能
- 导出Excel功能正常（下载 .xlsx 文件）
- 分页功能正常（首页、上页、下页、末页、跳转）
- 创建请假条弹窗表单验证正常（HTML5 required）
- 撤销请假确认对话框正常（confirm dialog）
- 审核弹窗（选择已批准/已拒绝 + 备注）正常
- 搜索/筛选/重置功能正常（审计日志页面验证）
- 退出登录功能正常（localStorage 清除，跳转登录页）

### 已知问题（非代码 Bug）
- 密码不一致: admin="1", reviewer="1", teacher="1", student="123456"
