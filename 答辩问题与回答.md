# 答辩问题与回答

1. **你的 JWT 鉴权是如何实现的？**
系统使用 python-jose 生成 JWT Token，登录成功后返回 Token，客户端每次请求在 Authorization header 携带。Token 过期后，前端检测到 401 状态码，引导用户重新登录。当前采用重新登录模式，而非 Token 刷新机制。

2. **Student 和 Reviewer 的关系是如何设计的？**
Student 归属 School，每个 Student 关联一个 Reviewer 作为审批人，Reviewer 可以审批多个 Student 的请假申请。这种设计符合实际业务中「辅导员审批学生请假」的场景，关系清晰且易于扩展。

3. **系统的请假审批流程是如何运转的？**
学生提交请假申请后状态为「待审批」，Reviewer 可以批准或拒绝，教师可以查看，审批完成后状态更新为「已批准」或「已拒绝」。请假销假和撤销功能也在审批流程中体现，形成了完整的闭环。

4. **系统支持哪些类型的请假？如何区分？**
系统支持四类型：事假、病假、公假、丧假。请假类型以中文字符串形式存储在数据库中，审批时可作为参考条件，类型设计覆盖了学生常见的请假场景。
