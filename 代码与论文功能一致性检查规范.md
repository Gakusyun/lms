# 高校学生请假管理系统 - 代码与论文功能一致性检查规范

> 本文档基于《基于微信小程序的高校学生请假管理系统的设计与实现》提取核心功能契约、业务流程、数据模型与接口规范。请将此文档提交给专业编码AI，用于逐项核对实际系统代码与论文描述的一致性。

请重点核查论文中提及但代码中尚未实现的功能。若因论文撰写较早导致功能描述与实际代码存在差异，请一律以实际代码为准。

我不需要刷新，因为我希望一直登录。不需要登录过期

## 一、 系统角色与权限定义（RBAC）
系统必须严格实现以下四类角色及其权限边界，权限校验需基于JWT令牌载荷中的角色标识进行拦截：
| 角色 | 核心权限与功能边界 |
|---|---|
| **学生 (Student)** | 提交请假申请、查看审批进度与历史记录、撤销“待审批”状态申请、查看个人基本信息与选课信息、展示/使用请假二维码 |
| **教师 (Teacher)** | 查看所授课程的学生出勤与请假统计、扫描二维码核验请假凭证真伪、导出课程相关请假数据 |
| **审核员 (Reviewer)** | 审批/驳回请假申请（支持单条与批量）、代提紧急请假并指定担保人、查看待办审批列表、填写审核意见、查看智能审批推荐、数据追溯复核 |
| **管理员 (Admin)** | 全局用户管理（学生/教师/审核员CRUD）、课程与选课管理、系统配置与安全策略调整、操作日志审计与多维检索、数据备份/恢复、批量数据导入导出 |

## 二、 核心功能模块清单
### 1. 用户认证与安全管理模块
- 密码采用 `bcrypt` 强哈希算法（含盐值）存储，支持密码修改与找回（或管理员后台导入初始化）。
- 集成二维码登录功能，动态生成扫码Token，移动端扫码完成身份验证。
- 全量API需实现基于角色的访问控制（RBAC），非法越权请求需被拦截并返回明确错误。

### 2. 请假申请管理模块
- 支持请假类型：事假、病假、公假、婚假、丧假。
- 必填字段校验：请假时间范围、事由、佐证材料（可选）。
- 自动业务校验：课程冲突检测、历史请假频次校验、时长分级校验（触发多级审批）。
- 状态流转枚举：`待审批` → `已批准` / `已拒绝` / `已撤销`。
- 紧急请假支持担保人机制（关联担保学生ID）。
- 学生端需实时展示审批进度与审核意见，所有操作留痕。

### 3. 审批流程管理模块
- 提供精细化单条审批与高效批量审批双模式。
- 审批时需关联学生基本信息、请假事由、时段、课程等上下文数据。
- 审核员可填写审批意见（`audit_remarks`），操作需实时触发状态同步与消息通知。
- 内置智能推荐模块：依据事由、频次、课程权重生成辅助审批建议。
- 审批通过后，系统自动为该请假记录生成唯一数字凭证（二维码）。

### 4. 二维码凭证与核验模块
- 仅对“已批准”状态的请假记录生成唯一、加密的二维码。
- 二维码内嵌关键请假信息，需设定明确的有效时间区间与使用次数限制。
- 核验端（教师/考勤人员）扫码后，后端需解密验证并返回：学生信息、请假时间、审批状态、有效性判定。
- 严格拦截过期、伪造、重复使用或状态异常的二维码，返回对应错误提示。

### 5. 数据管理与统计分析模块
- 支持多维度数据导出：用户信息、请假记录、审批日志等，格式需包含 `XLSX` 与 `JSON`。
- 内置统计分析：自动生成缺勤率、请假趋势、各学院/课程统计图表。
- 支持批量操作：新生信息批量导入、积压历史请假批量处理。
- 系统配置与日志管理：记录关键操作时间、行为、结果及关联对象，支持按时间/角色/类型检索与告警。

## 三、 核心业务流程规范
1. **请假申请流程**：学生提交申请 → 系统自动校验（时长/冲突/频次） → 根据规则分配至对应审核员 → 状态更新为“待审批” → 审批完成 → 状态同步至学生端 → 若批准则生成二维码。
2. **审批处理流程**：审核员登录 → 加载待审批列表 → 查看申请详情 → 执行审批（通过/驳回）或批量处理 → 填写意见 → 提交后系统更新状态、记录审计时间、触发通知、生成二维码（若通过）。
3. **二维码验证流程**：授权人员扫码 → 后端解析并校验签名/有效期/状态 → 返回验证结果（成功显示脱敏详情 / 失败提示无效/过期/伪造） → 更新核验日志。

## 四、 数据库核心实体与关联要求
系统需严格遵循第三范式，核心表结构及关键字段如下（需核对ORM模型/SQL建表语句）：
- `Admin`：`admin_id`(PK), `name`, `password`(bcrypt)
- `Student`：`student_id`(PK), `student_name`, `password`, `school_id`(FK), `reviewer_id`(FK), `guarantee_permission`
- `Teacher`：`teacher_id`(PK), `teacher_name`, `password`
- `Reviewer`：`reviewer_id`(PK), `reviewer_name`, `school_id`(FK), `role_id`(FK), `password`
- `School`：`school_id`(PK), `school_name`(唯一)
- `Role`：`role_id`(PK), `role_name`(唯一)
- `Login`：`login_id`(PK), `user_role`, `user_id`, `user_name`, `token`, `login_time`, `can_be_used`
- `Leave`：`leave_id`(PK), `student_id`(FK), `leave_date`, `leave_hours`, `status`(枚举), `leave_type`(枚举), `remarks`, `materials`, `reviewer_id`(FK), `teacher_id`(FK), `audit_remarks`, `audit_time`, `course_id`(FK), `is_modified`, `guarantee_student_id`(FK)
- `Course`：`course_id`(PK), `teacher_id`(FK), `course_name`, `class_hours`
- `StudentCourse`：`student_id`(PK+FK), `course_id`(PK+FK), `enrollment_date`, `status`
- **关联约束**：学生-请假(1:N)、学生-选课(1:N)、课程-选课(1:N)、教师-课程(1:N)、审核员-请假(1:N)。外键需严格对应，数据完整性需由ORM或数据库约束保障。

## 五、 RESTful API 接口契约
所有接口需遵循无状态原则，统一前缀 `/api/v1/`，采用JSON数据交换，需携带JWT Token（除登录/状态检查外）。
| 模块 | 方法 | 路径 | 核心参数/返回 | 功能要求 |
|---|---|---|---|---|
| 认证 | POST | `/auth/login` | `id, password, token` → `role, id, name, token` | 多角色登录，记录Login表 |
| 认证 | GET | `/auth/login/check` | `token` → 用户信息 | 校验登录状态 |
| 认证 | GET | `/auth/logout` | `token` → 结果 | 注销令牌 |
| 认证 | GET | `/auth/login/orcode` | `token` → `login_token` | 二维码登录 |
| 认证 | POST | `/auth/change-password` | 新旧密码 → 结果 | 修改密码 |
| 学生 | GET/POST | `/students`, `/students/{id}` | 分页参数/表单 → 列表/详情 | 增删改查，角色隔离 |
| 请假 | GET/POST | `/leaves`, `/leaves/{id}` | 分页/角色过滤 → 记录列表 | 按角色/课程/学生过滤 |
| 请假 | GET | `/leaves/student/{id}`<br>`/leaves/reviewer/{id}`<br>`/leaves/course/{id}` | 对应ID → 列表 | 多维度查询 |
| 教师 | GET/POST | `/teachers`, `/teachers/{id}` | 分页/表单 → 列表/详情 | 教师管理 |
| 课程 | GET/POST | `/courses`, `/courses/{id}` | 分页/表单 → 列表/详情 | 课程管理 |
| 审核员 | GET/POST | `/reviewers`, `/reviewers/{id}` | 分页/表单 → 列表/详情 | 审核员管理 |
| 选课 | POST/GET | `/student-courses`, `/student-courses/{type}/{id}` | 选课数据/关联ID → 记录 | 学生选课与关联查询 |
| 状态 | GET | `/status` | 无 → 系统状态 | 健康检查 |

## 六、 前后端功能映射要求
- **Web管理端 (Vue 3 + Axios)**：需实现登录页（含二维码登录）、用户管理看板、请假审批工作台、课程/选课配置页、数据统计可视化面板、日志审计页、多格式数据导出组件。HTTP请求需封装拦截器自动注入 `Authorization: Bearer <token>`。
- **微信小程序端 (Vue Mini + wx API)**：需实现首页统计看板、请假申请表单（含类型/时间/事由/材料）、请假记录列表（状态筛选）、二维码展示/调用扫码API、个人中心。需使用 `wx.setStorageSync` 管理Token与用户信息，支持分包加载优化首屏性能。

## 七、 AI 一致性检查执行清单
请编码AI按以下维度逐项核对代码库，输出《一致性差异报告》：
1. **架构与技术栈**：确认是否采用 FastAPI + SQLModel + JWT + Vue 3 + Vue Mini + SQLite/MySQL，依赖版本是否与论文配置一致。
2. **权限与认证**：核对JWT生成/解析逻辑、RBAC拦截中间件、bcrypt加密实现、Token过期与刷新机制是否完整实现。
3. **数据库模型**：逐字段核对ORM模型（`Student`, `Leave`, `Reviewer`等）与论文表结构是否一致，外键关联、枚举类型（状态/请假类型）、默认值、非空约束是否匹配。
4. **API路由与契约**：核对 `/api/v1/` 下所有路由路径、HTTP方法、请求参数校验（Pydantic）、响应数据结构是否与第四章接口表完全对应。
5. **核心业务逻辑**：
   - 请假申请是否实现课程冲突校验、时长分级路由、担保人字段关联？
   - 审批模块是否支持单条/批量操作、状态枚举流转、`audit_time` 与 `audit_remarks` 写入？
   - 二维码生成是否仅针对“已批准”状态？是否包含有效期/次数限制与加密校验逻辑？
   - 数据导出是否支持 XLSX/JSON？统计模块是否提供可视化数据聚合接口？
6. **前后端交互**：核对Web端与小程序端的Axios/wx.request封装、Token注入机制、错误提示交互、页面路由是否与论文描述一致。
7. **安全与规范**：核对是否实现CORS配置、参数化查询（防SQL注入）、输入过滤、操作日志记录、HTTPS部署准备等安全设计。

> **使用说明**：将实际系统源码目录结构、核心文件路径与上述规范逐项比对。若发现功能缺失、字段不一致、流程偏差、接口契约不匹配或安全逻辑未实现，请在差异报告中明确标注“论文描述 vs 代码实现”的对比结果，并给出修复建议。